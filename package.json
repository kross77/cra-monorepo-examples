{
  "name": "cra-monorepo-example",
  "version": "0.0.1",
  "main": "index.js",
  "repository": "",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clean": "lerna clean -f --yes && rm -rf node_modules",
    "reset": "yarn run clean && yarn",
    "editor": "cd apps/editor && yarn start",
    "quiz": "cd apps/quiz && yarn start"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^2.6.0"
  },
  "workspaces": [
    "apps/*",
    "comps/*",
    "cra-comps/*"
  ]
}
